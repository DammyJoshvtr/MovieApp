import { submitMovie } from "./useMetrics"

export const updateSearchCount = (query: string, movie: Movie) => {

  if (submitMovie)




  // check if the record of the search has already been stored
  // If the  document is found increment the searchCount Field
  // if no document is found, create a new a document ib firebase and initialize its count to one
}